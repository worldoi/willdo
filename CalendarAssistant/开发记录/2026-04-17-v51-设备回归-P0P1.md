# 2026-04-17 v51 设备回归（P0/P1）

## 1) 构建与安装

- 使用 Android Studio JDK 重新构建并安装：
  - `JAVA_HOME="/d/application/Android Studio/jbr" ./gradlew.bat :app:assembleDebug`
  - `adb install -r app/build/outputs/apk/debug/app-debug.apk`
- 结果：安装成功。

## 2) Provider CRUD 冒烟（单例 + 重复）

- 脚本：`scripts/provider_smoke_test.py`
- 报告：
  - `开发记录/provider_smoke_result_1776389238.json`
  - `开发记录/provider_smoke_result_1776391036.json`
- 结果：`9/9 PASS`
- 覆盖点：
  - 单例：新增 / 更新 / 删除
  - 重复：主系列新增、实例查询
  - 重复仅此更新（exception）
  - 重复仅此删除（canceled exception）
  - 重复仅以后（截断 + 新主线）
  - 重复整组删除

## 3) App 动作语义回归（终态动作 + Undo + 重复实例）

- 新增脚本：`scripts/device_action_test.py`
- 报告：
  - `开发记录/device_action_result_1776389935.json`
  - `开发记录/device_action_result_1776391069.json`
- 日志摘录：
  - `开发记录/device_action_logs_1776389935.log`
  - `开发记录/device_action_logs_1776391069.log`
- 结果：`17/17 PASS`（复跑一致）
- 覆盖点：
  - 单例动作：
    - `train`：已检票 -> 撤销检票
    - `food/pickup/ticket/sender/taxi`：已完成 -> 撤销完成
  - 重复实例：
    - `general`：仅单日完成（生成 exception，parent 增加 excluded）
    - `pickup`：重复实例终态动作被拦截（单日限制生效）
  - Undo 时效：
    - 60 秒内可撤销
    - 超过 60 秒撤销失效（状态保持完成）

## 4) 重复日程编辑/删除范围回归（this/future/all）

- 新增脚本：`scripts/device_recurring_scope_test.py`
- 代码辅助（仅 Debug 生效）：
  - `app/src/main/java/com/antgskds/calendarassistant/service/receiver/EventActionReceiver.kt`
  - 新增 `ACTION_DEBUG_RECURRING_DELETE_SCOPE`
  - Debug action 统一加 `BuildConfig.DEBUG` 守卫
- 报告：`开发记录/device_recurring_scope_result_1776390904.json`
- 日志：`开发记录/device_recurring_scope_logs_1776390904.log`
- 结果：`7/7 PASS`
- 覆盖点：
  - 编辑：`仅此 / 仅以后 / 全部`
  - 删除：`仅此 / 仅以后 / 全部`

## 5) 通知/胶囊联动回归（调度与取消）

- 新增脚本：`scripts/device_notification_link_test.py`
- 报告：`开发记录/device_notification_result_1776391007.json`
- 日志：`开发记录/device_notification_logs_1776391007.log`
- 结果：`5/5 PASS`
- 验证结论：
  - 新增测试事件后，`ACTION_CAPSULE_START/REFRESH/END` 计数按预期 +1
  - 完成事件后，计数恢复基线（`cancelReminders` 联动生效）

## 6) 数据库/状态核对

- 测后应用内事件状态：
  - `total=74, recurring=24, recurringParent=6, exception=0, completed=0`
  - `archives=0`
- QA 注入数据清理：
  - `qa_*` 残留 `0`

## 7) 日志观察

- `CrashLogs/exception.log` 存在历史与本轮触发的无障碍守护取消日志：
  - `AccessibilityGuardian Enable accessibility failed`
  - `JobCancellationException`
- 该日志未阻断本轮 P0/P1 测试结果（功能链路均通过）。

## 8) 结论

- 本轮要求的核心链路（单例 CRUD、重复 CRUD 主语义、重复单日完成、单例终态动作）已在设备上通过。
- Undo 语义（动作类型匹配 + 60 秒时效）通过。
- 编辑/删除 `this/future/all` 范围语义与通知/胶囊调度取消联动已补测通过。
- 当前可进入下一阶段：UI 侧纯可视补充回归（弹窗文案、交互反馈、布局细节）。
