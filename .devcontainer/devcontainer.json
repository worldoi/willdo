{
  "name": "Will-do / CalendarAssistant (Android)",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-22.04",
  "remoteUser": "root",
  "customizations": {
    "vscode": {
      "extensions": [
        "vscjava.vscode-gradle",
        "galudisu.android",
        "JetBrains.kotlin",
        "redhat.java"
      ]
    }
  },
  "onCreateCommand": "apt-get update -y && apt-get install -y --no-install-recommends openjdk-17-jdk-headless curl unzip && chmod +x ./start.sh CalendarAssistant/gradlew 2>/dev/null || true",
  "hostRequirements": {
    "cpus": 2,
    "memory": "4gb"
  },
  "forwardPorts": []
}
